# Server Configuration
# Use 3000 for local development, 9876 for production
PORT=3000
NODE_ENV=development

# Database
DATABASE_PATH=./food-search.db

# Session
SESSION_SECRET=food-search-secret-key-2024-change-this-in-production

# JWT Configuration
JWT_SECRET=your-secure-jwt-secret-change-in-production
JWT_EXPIRES_IN=7d

# Default Admin Account Configuration (OPTIONAL)
# Only needed if you want to create an initial admin via environment variables
# Otherwise, admins can be invited via the invitation system
# SECURITY WARNING: Set a strong password if using this method!
# Password must be at least 8 characters long
# ADMIN_EMAIL=sergiu.tigan@devhub.tech
# ADMIN_PASSWORD=your-secure-admin-password-here

# Email Configuration (Optional - for notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=Food Search <noreply@devhub.tech>

# Application URL (for email links)
APP_URL=http://localhost:3000

# Frontend URL (for invitation links)
FRONTEND_URL=http://localhost:4200

# Allowed Email Domains (comma-separated)
ALLOWED_EMAIL_DOMAINS=@devhub.tech,@titans.net,@solidstake.com

# CORS Configuration (comma-separated origins)
# Include all domains that will access the API (with http:// or https://)
CORS_ORIGIN=http://localhost:3000,http://localhost:4200,https://food.tigan.dev,http://food.tigan.dev
